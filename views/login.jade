include mixins
extends layout

block append scripts
    script(type='text/javascript').
        $(document).ready(function () {
            //$('#page').fadeOut(1000, function () {

            //});
        });

block content
    #page
        h1 Login
            form(method="post", action="/api/signup").inputform
                +inputFormGlyph("name","text", "Username", "user", "")
                +inputFormGlyph("password","password", "Password", "console", "")
                input(type="submit")

            +buttonGroup("").center
                +buttonGroupElement("Login")
                +buttonGroupElement("Register")


        <div ng-controller="UserLogin">
        <span></span>

        <input ng-model="user.username" type="text" name="user" placeholder="Username" />
        <input ng-model="user.password" type="password" name="password" placeholder="Password" />
        <button ng-click="submit()" value="Login">Login</button>
        <p> {{message}} </p>

        </div>